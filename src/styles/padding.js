const paddingBase = '12';

export const PADDING = {
  X_SMALL: `${paddingBase / 4}px`,
  SMALL: `${paddingBase / 2}px`,
  BASE: `${paddingBase}px`,
  BASE_LARGER: `${paddingBase * 1.5}px`,
  LARGE: `${paddingBase * 2}px`,
  X_LARGE: `${paddingBase * 3}px`,
};
